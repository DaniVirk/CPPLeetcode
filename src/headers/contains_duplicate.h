#ifndef CONTAINS_DUPLICATE_H
#define CONTAINS_DUPLICATE_H
#include <vector>

using std::vector;

class contains_duplicate {
public:
    static bool hasDuplicate(vector<int>& nums);
};



#endif //CONTAINS_DUPLICATE_H
